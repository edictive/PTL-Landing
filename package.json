{
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build:css": "tailwindcss -i ./src/styles.css -o ./assets/tailwind.css -m",
    "subset:fonts": "node scripts/subset-fonts.js",
    "build": "npm run build:css && npm run subset:fonts && node scripts/fingerprint-assets.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.10",
    "prettier": "3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "tailwindcss": "^4.1.10"
  }
}
